@model EmployeeList_MVC.Models.Employee
@{
    Layout = null;
}


<div>
    <h3>Delete Employee</h3>

    <p>Are you sure you want to delete the employee data of
        <span style="font-weight:bold"> @Html.DisplayFor(model => model.FirstName)  @Html.DisplayFor(model => model.LastName)?</span>
    </p>

    <hr />
   
    <form asp-action="Delete" asp-route-id="@Model.ID"  class="d-inline">
        <input type="hidden" asp-for="@Model.ID" />
        <input type="submit" value="Delete" class="btn btn-danger" />
    </form>
   @*  <form asp-action="Delete">
        <input type="hidden" asp-for="ID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" class="btn btn-info">Back to List</a>
    </form> *@
</div>
